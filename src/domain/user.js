class User {
  constructor(id, fullname, email, nickname, password) {
    this.id = id;
    this.fullname = fullname;
    this.email = email;
    this.nickname = nickname;
    this.password = password;
  }
}

module.exports = User;
